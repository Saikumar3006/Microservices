spring.application.name=product-service
# /product-service  -> where the data lives
# ?authSource=admin -> where the 'root' account lives (the container creates it there)
spring.mongodb.uri=mongodb://root:password@localhost:27017/product-service?authSource=admin
